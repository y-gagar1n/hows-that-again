{
  "name": "gatsby-starter-default",
  "description": "Gatsby default starter",
  "version": "1.0.0",
  "author": "Kyle Mathews <mathews.kyle@gmail.com>",
  "dependencies": {
    "gatsby": "^2.15.1",
    "gatsby-link": "^2.2.10",
    "gatsby-plugin-react-helmet": "^3.1.5",
    "gatsby-remark-prismjs": "^3.3.11",
    "gatsby-source-filesystem": "^2.1.18",
    "gatsby-transformer-remark": "^2.6.19",
    "prismjs": "^1.17.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-helmet": "^5.2.0"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rimraf public && rimraf .cache && gatsby build",
    "develop": "gatsby develop",
    "format": "rimraf public && rimraf .cache && prettier --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages-clean && rimraf public && rimraf .cache && gatsby build --prefix-paths && gh-pages -d public -r https://github.com/y-gagar1n/hows-that-again.git",
    "heroku-postbuild": "npm run deploy"
  },
  "devDependencies": {
    "gatsby-plugin-sharp": "^2.2.18",
    "gatsby-remark-images": "^3.1.19",
    "gh-pages": "^1.2.0",
    "prettier": "^1.12.0",
    "rimraf": "^2.6.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gatsbyjs/gatsby-starter-default"
  }
}
