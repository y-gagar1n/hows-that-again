webpackJsonp([0x824449b8e431],{420:function(e,n){e.exports={data:{markdownRemark:{html:'<p><a href="http://c-faq.com/decl/spiral.anderson.html">Spiral rule</a></p>\n<h1>Suggestions for C++ programmers (C++11 features)</h1>\n<ol>\n<li>Use constructors to establish invariants (§2.4.3.2, §13.4, §17.2.1).</li>\n<li>Use constructor/destructor pairs to simplify resource management (RAII; §5.2, §13.3).</li>\n<li>Avoid ‘‘naked’’ new and delete (§3.2.1.2, §11.2.1).</li>\n<li>Use containers and algorithms rather than built-in arrays and ad hoc code (§4.4, §4.5,\n§7.4, Chapter 32).</li>\n<li>Prefer standard-library facilities to locally developed code (§1.2.4).</li>\n<li>Use exceptions, rather than error codes, to report errors that cannot be handled locally\n(§2.4.3, §13.1).</li>\n<li>Use move semantics to avoid copying large objects (§3.3.2, §17.5.2).</li>\n<li>Use unique_ptr to reference objects of polymorphic type (§5.2.1).</li>\n<li>Use shared_ptr to reference shared objects, that is, objects without a single owner that is\nresponsible for their destruction (§5.2.1).</li>\n<li>Use templates to maintain static type safety (eliminate casts) and avoid unnecessary use\nof class hierarchies (§27.2).</li>\n</ol>\n<h1>Suggestions for C Programmers</h1>\n<ol>\n<li>Don’t think of C++ as C with a few features added. C++ can be used that way, but only\nsuboptimally. To get really major advantages from C++ as compared to C, you need to\napply different design and implementation styles.</li>\n<li>Don’t write C in C++; that is often seriously suboptimal for both maintenance and perfor-\nmance.</li>\n<li>Use the C++ standard library as a teacher of new techniques and programming styles.\nNote the difference from the C standard library (e.g., = rather than strcpy() for copying\nand == rather than strcmp() for comparing).</li>\n<li>Macro substitution is almost never necessary in C++. Use const (§7.5), constexpr (§2.2.3,\n§10.4), enum or enum class (§8.4) to define manifest constants, inline (§12.1.5) to avoid\nfunction-calling overhead, template s (§3.4, Chapter 23) to specify families of functions\nand types, and namespace s (§2.4.2, §14.3.1) to avoid name clashes.</li>\n<li>Don’t declare a variable before you need it, and initialize it immediately. A declaration\ncan occur anywhere a statement can (§9.3), in for -statement initializers (§9.5), and in con-\nditions (§9.4.3).</li>\n<li>Don’t use malloc() . The new operator (§11.2) does the same job better, and instead of\nrealloc() , try a vector (§3.4.2). Don’t just replace malloc() and free() with ‘‘naked’’ new and\ndelete (§3.2.1.2, §11.2.1).</li>\n<li>Avoid void∗ , unions, and casts, except deep within the implementation of some function\nor class. Their use limits the support you can get from the type system and can harm per-\nformance. In most cases, a cast is an indication of a design error. If you must use an\nexplicit type conversion, try using one of the named casts (e.g., static_cast ; §11.5.2) for a\nmore precise statement of what you are trying to do.</li>\n<li>Minimize the use of arrays and C-style strings. C++ standard-library string s (§4.2), array s\n(§8.2.4), and vector s (§4.4.1) can often be used to write simpler and more maintainable\ncode compared to the traditional C style. In general, try not to build yourself what has\nalready been provided by the standard library.</li>\n<li>Avoid pointer arithmetic except in very specialized code (such as a memory manager) and\nfor simple array traversal (e.g., ++p ).</li>\n<li>Do not assume that something laboriously written in C style (avoiding C++ features such\nas classes, templates, and exceptions) is more efficient than a shorter alternative (e.g.,\nusing standard-library facilities). Often (but of course not always), the opposite is true.</li>\n</ol>\n<h1>Suggestions for Java/C# Programmers</h1>\n<ol>\n<li>Don’t simply mimic Java style in C++; that is often seriously suboptimal for both main-\ntainability and performance.</li>\n<li>Use the C++ abstraction mechanisms (e.g., classes and templates): don’t fall back to a C\nstyle of programming out of a false feeling of familiarity.</li>\n<li>Use the C++ standard library as a teacher of new techniques and programming styles.</li>\n<li>Don’t immediately invent a unique base for all of your classes (an Object class). Typi-\ncally, you can do better without it for many/most classes.</li>\n<li>Minimize the use of reference and pointer variables: use local and member variables\n(§3.2.1.2, §5.2, §16.3.4, §17.1).</li>\n<li>Remember: a variable is never implicitly a reference.</li>\n<li>Think of pointers as C++’s equivalent to Java references (C++ references are more lim-\nited; there is no reseating of C++ references).</li>\n<li>A function is not virtual by default. Not every class is meant for inheritance.</li>\n<li>Use abstract classes as interfaces to class hierarchies; avoid "brittle base classes", that is, base classes with data members.</li>\n<li>Use scoped resource management ("Resource Acquisition Is Initialization"; RAII) when-\never possible.</li>\n<li>Use a constructor to establish a class invariant (and throw an exception if it can’t).</li>\n<li>If a cleanup action is needed when an object is deleted (e.g., goes out of scope), use a de-\nstructor for that. Don’t imitate finally (doing so is more ad hoc and in the longer run far\nmore work than relying on destructors).</li>\n<li>Avoid "naked" <code>new</code> and <code>delete</code> ; instead, use containers (e.g., <code>vector</code>, <code>string</code>, and <code>map</code>) and handle classes (e.g., <code>lock</code> and <code>unique_ptr</code> ).</li>\n<li>Use freestanding functions (nonmember functions) to minimize coupling (e.g., see the\nstandard algorithms), and use namespaces (§2.4.2, Chapter 14) to limit the scope of free-\nstanding functions.</li>\n<li>Don’t use exception specifications (except <code>noexcept</code>; §13.5.1.1).</li>\n<li>A C++ nested class does not have access to an object of the enclosing class.</li>\n<li>C++ offers only the most minimal run-time reflection: <code>dynamic_cast</code> and <code>typeid</code> (Chapter\n22). Rely more on compile-time facilities (e.g., compile-time polymorphism; Chapter 27,\nChapter 28).</li>\n</ol>\n<h1>Ключевые слова</h1>\n<h2>constexpr</h2>\n<p><code>const</code> - константное выражение, которое может быть вычислено на этапе выполнения.</p>\n<p><code>constexpr</code> - константное выражение, которое должно быть вычислено на этапе компиляции. Необходимо для экономии ресурсов, а так же в некоторых синтаксических конструкциях. </p>\n<ul>\n<li>\n<p>Переменная может быть объявлена как <code>constexpr</code>, если она имеет <strong>литеральный тип</strong> и инициализирована. Если инициализация осуществляется через конструктор, то конструктор должен быть помечен как <code>constexpr</code>. </p>\n</li>\n<li>\n<p>Ссылка может быть объявлена как <code>constexpr</code>, если объект, на который она ссылается инициализирован константным выражением и все преобразования, выполняемые при инициализации, тоже являются константными выражениями.</p>\n</li>\n</ul>\n<h1>Указатели</h1>\n<p>Проверка, указывает ли указатель на объект:</p>\n<p><code>if (p==nullptr) return 0;</code></p>\n<h2>Разница между указателями и ссылками</h2>\n<ol>\n<li>Указатель может быть переназначен сколько угодно раз, ссылка - не может.</li>\n<li>Указатель может указывать в никуда (<code>nullptr</code>), ссылка - не может.</li>\n<li>Адрес ссылки получить нельзя.</li>\n<li>Нет "арифметики ссылок".</li>\n<li>Можно иметь указатель на указатель на указатель и т.д. У ссылок может быть только один уровень.</li>\n<li>Чтобы получить значение по ссылке, не нужно использовать символы * и ->.</li>\n</ol>\n<h2>Когда использовать ссылки</h2>\n<ul>\n<li>аргументы функций</li>\n<li>возвращаемые значения функций</li>\n</ul>\n<h2>Когда использовать указатели</h2>\n<ul>\n<li>в алгоритмах</li>\n<li>в структурах данных</li>\n</ul>\n<h1>Конструктор инициализации</h1>\n<pre><code>class Vector {\npublic:\n    Vector(int s) :elem{new double[s]}, sz{s} {}\nprivate:\n    double* elem;\n    int sz;\n};\n</code></pre>\n<p>Здесь запись <code>Vector(int s) :elem{new double[s]}, sz{s} {}</code> означает, что при использовании этого конструктора нужно приватные поля <code>elem</code> и <code>sz</code> инициализировать указанными значениями.</p>\n<h1>Ассершны</h1>\n<p>Ассерты в рантайме осуществляются командой <code>static_assert</code>:</p>\n<p><code>static_assert(4&#x3C;=sizeof(int), "Integers are too small");</code></p>\n<h1>RAII</h1>\n<p>Формулируется так: владение ресурсом есть инварианта класса. Это означает, что захват ресурса должен производиться в конструкторе, а освобождение - в деструкторе.</p>\n<p>Рантайм обеспечивает, что деструктор вызывается только, если конструктор не возбудил исключения. Поэтому если в конструкторе не удалось захватить ресурс - нужно стрелять исключение. Если в деструкторе не удалось освободить - тоже стрелять исключение.</p>\n<h1>initializer_list</h1>\n<p>Если мы инициализируем контейнер следующей конструкцией:</p>\n<p><code>Vector v = {1,2,3,4,5};</code></p>\n<p>то у него вызывается вот такой конструктор:</p>\n<p><code>Vector(initializer_list&#x3C;double> lst)</code></p>\n<h2>Пример реализации</h2>\n<pre><code class="language-cpp">#include &#x3C;initializer_list>\n#include &#x3C;algorithm>\n\nVector(std::initializer_list&#x3C;double> lst) {\n    std::copy(lst.begin(), lst.end(), elem);\n}\n</code></pre>\n<h1>Типы и префиксы строк</h1>\n<ul>\n<li><strong>char</strong> - 8-битные символы, нужен для ASCII и UTF-8</li>\n<li><strong>char16_t</strong> - 16-битные символы, нужен для UTF-16</li>\n<li><strong>char32_t</strong> - 32-битные символы, нужен для UTF-32</li>\n<li><strong>wchar_t</strong> - любые "широкие" символы (wide characters), то есть символы с размером, большим чем 8 бит. Имеет размер, достаточный для того, чтобы представить любой поддерживаемый символ Unicode.</li>\n</ul>\n<p>Префиксы:</p>\n<table>\n<thead>\n<tr>\n<th align="right">Префикс</th>\n<th>Тип</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td align="right">"Hello"</td>\n<td>const char*</td>\n</tr>\n<tr>\n<td align="right">L"Hello"</td>\n<td>const wchar_t*</td>\n</tr>\n<tr>\n<td align="right">u8"Hello"</td>\n<td>const char* / const char8_t*</td>\n</tr>\n<tr>\n<td align="right">u"Hello"</td>\n<td>const char16_t*</td>\n</tr>\n<tr>\n<td align="right">U"Hello"</td>\n<td>const char32_t*</td>\n</tr>\n</tbody>\n</table>\n<p>Пример:</p>\n<pre><code>#include &#x3C;iostream>\n#include &#x3C;string>\nusing namespace std;\n\nint main()\n{\n    cout &#x3C;&#x3C; "\\n\\n Hello World! (plain) \\n";\n    cout &#x3C;&#x3C; u8"\\n Hello World! (u8) \\n";\n    cout &#x3C;&#x3C; u"\\n Hello World! (u) \\n";\n    cout &#x3C;&#x3C; U"\\n Hello World! (U) \\n";\n    cout &#x3C;&#x3C; L"\\n Hello World! (plain) \\n\\n";\n\n    cout &#x3C;&#x3C; "\\n\\n\\n";\n}\n</code></pre>\n<p>3-5 <code>cout</code> вернут ерунду типа <code>0x47f0d8</code>. Так происходит из-за того, что оператор <code>&#x3C;&#x3C;</code> для класса <code>std::cout</code> принимает <code>const char*</code>, но не содержит перегрузок для <code>const char16_t*</code>, <code>const char32_t*</code>, <code>const wchar_t*</code>. Поэтотому он применит перегрузку для печати указателей.</p>\n<p>Однако для вывода последнего есть специальный класс: <code>std::wcout</code>.</p>\n<h1>Инициализация</h1>\n<p>Есть 4 синтаксических стиля инициализации объектов:</p>\n<pre><code class="language-cpp">X a1{v};\nX a2 = {v};\nX a3 = v;\nX a4(4);\n</code></pre>\n<p>Самая предпочтительная - первая, через <code>{}</code>. Она же и самая новая - появилась в C++11.</p>\n<p>Ее преимущество в том, что она не допускает "сужения" типов, то есть:</p>\n<ul>\n<li>char приводить к int можно, наоборот - нельзя</li>\n<li>float приводить к double можно, наоборот - нельзя</li>\n<li>число с плавающей точкой нельзя приводить к целому</li>\n<li>целое число нельзя приводить к числу с плавающей точкой</li>\n</ul>\n<p>Однако если используется <code>auto</code>, то лучше использовать форму <code>a3</code>, то есть через <code>=</code>. Во-первых, в этом случае инициализация через <code>{}</code> не дает никаких преимуществ, а во-вторых тип может быть определен не так, как мы хочем:</p>\n<pre><code class="language-cpp">auto z1 {99}; // initializer_list&#x3C;int>\nauto z2 = 99; // int\n</code></pre>\n<p>Пустой список инициализации <code>{}</code> присвоит дефолтное значение:</p>\n<pre><code class="language-cpp">int x{}; // 0\nchar* p {}; // nullptr\nchar buf[1000] {}; // массив из тысячи нулей\n</code></pre>\n<h1>Алиасы типов</h1>\n<h2>using</h2>\n<pre><code class="language-cpp">using Pchar = char*;\nusing PF = int(*)(double);\n</code></pre>\n<p>Можно эти алиасы даже объявлять внутри типов и обращаться потом извне:</p>\n<pre><code class="language-cpp">template&#x3C;class T>\nclass vector {\npublic:\n    using value_type = T;\n    //...\n};\n\nvector&#x3C;int>::value_type vt; //int\n</code></pre>\n<h2>typedef</h2>\n<p>Это более старый синтаксис, служащий в тех же целях. Но он сложнее в понимании, потому что объявляемое имя стоит там, где стояло бы имя переменной при ее объявлении. </p>\n<p>Примеры:</p>\n<pre><code class="language-cpp">typedef int int32_t; // using int32_t = int;\ntypedef short int16_t; // using unt16_t = short;\ntypedef void(*PtoF)(int) // using PtoF = void(*)(int);\n</code></pre>\n<p>Суффикс <code>_t</code> в названиях типов по соглашению используется для алиасов.</p>\n<h1>Константные указатели</h1>\n<p>Если константным должен быть сам указатель, то вместо <code>*</code> нужно использовать <code>*const</code>:</p>\n<pre><code class="language-cpp">char* p;\nchar s[] = "Hello";\n\nconst char* pc = s; // указатель на константу\npc[3] = \'g\'; // ОШИБКА\npc = p;      // ok\n\nchar *const cp = s; // константый указатель\ncp[3] = \'g\'; // ok\ncp = p;      // ОШИБКА\n\nconst char *const cpc = s; // константный указатель на константу\ncpc[3] = \'g\';  // ОШИБКА\ncpc = p;       // ok\n</code></pre>\n<p>Если ошибиться и вместо <code>*const</code> написать <code>const*</code>, то это опять будет указатель на константу:</p>\n<pre><code class="language-cpp">char *const cp;  // константный указатель\nchar const* pc;  // указатель на константу\nconst char* pc2; // опять указатель на константу\n</code></pre>\n<p>Удобнее читать эти определения справа налево: <code>cp</code> это константый указатель на <code>char</code>, а <code>pc2</code> это указатель на <code>char const</code>.</p>\n<h1>Конструктор</h1>\n<p>Если у класса есть конструктор, принимающий один аргумент, то этот конструктор будет вызываться при инициализации аргументов этого типа.</p>\n<p>Пример:</p>\n<pre><code class="language-cpp">class uri {\npublic:\n    uri(const char* s): _c{s} {}\nprivate:\n    const char* _c;\n};\n\nuri u = "http://www.ya.ru";   // будет вызван конструктор\n</code></pre>\n<h1>Копирование объектов</h1>\n<p>По дефолту при присваивании происходит полное копирование всех членов объекта.</p>\n<p>Например,</p>\n<pre><code class="language-cpp">1 Vector bad_copy(Vector v1) \n2 {\n3   Vector v2 = v1;\n4   v1[0] = 2;   // v2[0] теперь тоже равен 2\n5   v2[1] = 3;   // v1[1] теперь тоже равен 3\n6   return v2;\n7 }\n</code></pre>\n<p>В большинстве случаев это совсем не то, чего мы хотели, так как это может нарушить RAII. Простое правило - если у класса есть деструктор, то это скорее всего означает, что дефолтная семантика копирования нам не подходит.</p>\n<p>Варианта решения два: <em>конструктор копирования</em> и <em>присваивание копированием</em>.</p>\n<h2>Конструктор копирования</h2>\n<p>Этот конструктор будет неявно вызываться каждый раз, когда мы передаем значение в функцию (не по ссылке), либо возвращаем из функции (опять же не по ссылке).</p>\n<pre><code class="language-cpp">Vector::Vector(const Vector&#x26; a)\n    :elem{new double[sz]},\n    sz{a.sz}\n{\n    for(int i=0; i&#x3C;sz; ++i)\n        elem[i]=a.elem[i];\n}\n</code></pre>\n<p>В вышеуказанном примере конструктор копирования будет вызван в строчках 1 и 6.</p>\n<h2>Присваивание копированием</h2>\n<p>Этот метод будет вызываться, когда происходит присваивание не по ссылке.</p>\n<pre><code class="language-cpp">Vector&#x26; Vector::operator=(const Vector&#x26; a)\n{\n    double* p = new double[a.sz];\n    for(int i=0; i &#x3C; sz; ++i)\n        p[i]=a.elem[i];\n    delete[] elem;\n    elem=p;\n    sz=a.sz;\n    return *this;\n}\n</code></pre>\n<p>В вышеуказанном примере конструктор копирования будет вызван в строчке 3.</p>\n<h1>Перемещение объектов</h1>\n<p>Допустим, у нас есть такой код:</p>\n<pre><code class="language-cpp">1 Vector operator+(const Vector&#x26; a, const Vector&#x26; b)\n2 {\n3     Vector res(a.size());\n4     for (int i=0; i!=a.size(); ++i)\n5         res[i]=a[i]+b[i];\n6     return res;\n7 }\n</code></pre>\n<p>Аргументы передаются по ссылке, тут все нормально. Но возвращать ссылку мы не можем (переменная на стеке res будет уничтожена как только мы выйдем из функции). Поэтому возвращаем значение и тут опять будет вызван конструктор копирования. Если вектор большой, то это будет весьма затратная операция.</p>\n<p>Более того, если клиент будет использовать суммирование так:</p>\n<p><code>Vector v = v1 + v2 + v3</code></p>\n<p>то в процессе будет осуществлено как минимум одно побочное копирование, которое нам в конечном счете совсем не нужно.</p>\n<p>Чтобы избежать копирования большого количества полей объекта, можно реализовать семантику перемещения через <em>конструктор перемещения</em> или <em>присваивание перемещением</em>. </p>\n<p>Обе эти конструкции используют <code>&#x26;&#x26;</code>, что означает не ссылку на ссылку, а <strong>rvalue reference</strong>, то есть ссылку, к которой можно привязать правую часть выражения (об этом позже).</p>\n<p>Реализуется конструктор/присваивание таким образом, чтобы нужные поля "переместились" из правой части в левую, то есть из правой части значения пропадают, а в левой - появляются.</p>\n<p>Также мы можем "помочь" компилятору, указав явное перемещение командой <code>std::move</code>.</p>\n<h2>Конструктор перемещения</h2>\n<p>Имеет сигнатуру <code>Vector(Vector&#x26;&#x26; a)</code>.</p>\n<p>Пример реализации:</p>\n<pre><code class="language-cpp">Vector::Vector(Vector&#x26;&#x26; a)\n    :elem{a.elem},\n    sz{a.sz}\n{\n    a.elem = nullptr;\n    a.sz = 0;\n}\n</code></pre>\n<p>В указанном примере компилятор распознает возможность и вызовет конструктор перемещения в строке 6.</p>\n<h2>Присваивание перемещением</h2>\n<p>Имеет сигнатуру <code>Vector&#x26; operator=(Vector &#x26;&#x26; a)</code>.</p>\n<h1>Шаблоны</h1>\n<p>Синтаксис такой:</p>\n<pre><code class="language-cpp">template&#x3C;typename T>\nclass Vector {\nprivate:\n    T* elem;\n    ...\n}\n</code></pre>\n<p>Причем если есть функции, определенные снаружи скоупа класса, то для них шаблон должен быть определен точно так же:</p>\n<pre><code class="language-cpp">template&#x3C;typename T>\nT&#x26; Vector&#x3C;T>::operator[](int i) const\n{\n    return elem[i];\n}\n</code></pre>\n<p>Причем придется так писать даже если тип внутри метода не используется:</p>\n<p>И еще <strong>важное примечание</strong>. Шаблонные реализации методов <strong>не могут</strong> быть описаны в файле .cpp, только в .h! Поэтому если у нас в .h заголовке описывается шаблонный класс, то это нормально, если у него нет соответствующего .cpp файла и все реализации описаны прямо в заголовке!</p>\n<p>Причина этого - в том, что все .cpp файлы компилируются по отдельности (а потом соединяются воедино линковщиком). В .cpp файле у нас, допустим, встречается <code>Vector&#x3C;int></code>. Чтобы скомпилировать эту строчку, компилятору нужно сгенерить новый класс, а для этого он должен видеть реализацию шаблона. Если реализация описана в другом файле .cpp, то на данном этапе у компилятора нет к нему доступа, а есть только к тем, которые описаны в текущем файле в секциях <code>#include</code> (обычно это файлы заголовков). Когда же он дойдет до компиляции того файла .cpp, в котором описана реализация шаблона, то он уже забудет, что нужно было сгенерить <code>Vector&#x3C;int></code>, так как (см. выше) все файлы компилятся отдельно.</p>\n<p>Еще часто делают так:</p>\n<pre><code class="language-cpp">// Foo.h\ntemplate &#x3C;typename T>\nstruct Foo\n{\n    void doSomething(T param);\n};\n\n#include "Foo.tpp"\n\n// Foo.tpp\ntemplate &#x3C;typename T>\nvoid Foo&#x3C;T>::doSomething(T param)\n{\n    //implementation\n}\n</code></pre>\n<h1>Вариативные шаблоны</h1>\n<p>Вариативные шаблоны - это шаблоны с заранее неизвестным числом аргументов.</p>\n<p>Пример:</p>\n<pre><code class="language-cpp">template&#x3C;typename T, typename ... Tail>\nvoid f(T head, Tail... tail)\n{\n    g(head); // сделать что-то с первым параметром\n    f(tail...); // рекурсивно вызвать себя с оставшимися параметрами\n}\n\nvoid f() { }  // когда ничего не осталось в списке типов - не делать ничего\n\ntemplate&#x3C;typename T>\nvoid g(T x)\n{\n    cout &#x3C;&#x3C; x &#x3C;&#x3C; " ";\n}\n\nf(1, 0.2, "hello")  // выведет 1 0.2 hello\n</code></pre>\n<h1>Функторы</h1>\n<p>Функторы - это объекты-функции, то есть объекты, которые могут быть вызваны как функции.</p>\n<p>Чтобы создать функтор, нужно всего лишь у класса переопределить ооператор <code>()</code>.</p>\n<pre><code class="language-cpp">template&#x3C;typename T>\nclass Less_than {\n    const T val;  // value to compare against\npublic:\n    Less_than(const T&#x26; v) :val(v) { }\n    bool operator()(const T&#x26; x) const { return x&#x3C;val; } // call operator\n};\n\nLess_than&#x3C;int> lti {42};\n// lti(i) will compare i to 42 using &#x3C; (i&#x3C;42)\nLess_than&#x3C;string> lts {"Backus"}; // lts(s) will compare s to "Backus" using &#x3C; (s&#x3C;"Backus")\n\nvoid fct(int n, const string &#x26; s)\n{\n    bool b1 = lti(n);  // true if n&#x3C;42\n    bool b2 = lts(s);  // true if s&#x3C;"Backus"\n    // ...\n}\n</code></pre>\n<h1>Лямбды</h1>\n<p>Лямбды описываются так:</p>\n<p><code>[&#x26;](int a){ return a&#x3C;x; }</code></p>\n<p>Здесь <code>[&#x26;]</code> - это capture list, или лист захвата. Возможные значения:</p>\n<ul>\n<li><code>[&#x26;]</code> что все переменные, используемые в теле лямбды, будут переданы по ссылке</li>\n<li><code>[&#x26;x]</code> - передается только <code>x</code>, по ссылке</li>\n<li><code>[=]</code> что все переменные, используемые в теле лямбды, будут переданы в виде копий</li>\n<li><code>[=x]</code> - передается только <code>x</code>, в виде копии</li>\n<li><code>[]</code> - не передается ничего</li>\n</ul>\n<p>Принимать лямбду в качестве аргумента можем, используя тип <code>std::function&#x3C;R(Args...)></code>, где <code>R</code> - тип результата, а <code>Args...</code> - типы аргументов.</p>\n<p>Пример:</p>\n<pre><code class="language-cpp">void f(const std::function&#x3C;void(string)> callback); // принимаем функцию, принимающую string и возвращающую ничего\n</code></pre>\n<h1>Стандартная библиотека</h1>\n<h2>Коллекции</h2>\n<ul>\n<li><code>vector&#x3C;T></code> - стандартный массив с динамическим размером. Проверок на выход за границы не делает, выдает мусор в этом случае</li>\n<li><code>list&#x3C;T></code> - двойной связанный список</li>\n<li><code>forward_list&#x3C;T></code> - одинарный связанный список</li>\n<li><code>deque&#x3C;T></code> - очередь с двумя концами</li>\n<li><code>set&#x3C;T></code> - множество</li>\n<li><code>multiset&#x3C;T></code> - множество, в котором значение может встречаться несколько раз</li>\n<li><code>map&#x3C;K,V></code> - ассоциативный массив, реализованный в виде красно-черного дерева поиска. Сложность поиска - <strong>O(log(n))</strong>.</li>\n<li><code>multimap&#x3C;K,V></code> - ассоциативный массив, в котором значение может встречаться несколько раз</li>\n<li><code>unordered_map&#x3C;K,V></code> - ассоциативный массив, реализованный в виде хэш-таблицы.</li>\n<li><code>unordered_multimap&#x3C;K,V></code> - ассоциативный мульти-массив, реализованный в виде хэш-таблицы.</li>\n<li><code>unordered_set&#x3C;T></code> - множество, реализованное как хэш-таблица</li>\n<li><code>unordered_multiset&#x3C;T></code> - мультимножество, реализованное как хэш-таблица</li>\n</ul>\n<p>Все эти коллекции представлены в заголовках: <code>&#x3C;vector>, &#x3C;list>, &#x3C;map></code>.</p>\n<p>Помимо перечисленных есть еще:</p>\n<ul>\n<li><code>queue&#x3C;T></code></li>\n<li><code>stack&#x3C;T></code></li>\n<li><code>priority_queue&#x3C;T></code></li>\n</ul>\n<h3>list</h3>\n<p>Вставка и удаление реализуются с использованием итераторов:</p>\n<pre><code class="language-cpp">void f(const Entry&#x26; ee, list&#x3C;Entry>::iterator p, list&#x3C;Entry>::iterator q)\n{\n    list.insert(p, ee);\n    list.erase(q);\n}\n</code></pre>\n<h3>map</h3>\n<p>Инициализация:</p>\n<pre><code class="language-cpp">map&#x3C;string, int> book {\n    {"Ann", 123},\n    {"John", 456}\n};\n</code></pre>\n<h3>unordered_map</h3>\n<p>Инициализация такая же, как и у <code>map</code>.</p>\n<p>Вставка:</p>\n<h3>Итерирование по любой коллекции</h3>\n<p>Через begin-end:</p>\n<pre><code class="language-cpp">for(auto p = list.begin(); p!=list.end(); ++p)\n    cout &#x3C;&#x3C; p->name;\n</code></pre>\n<p>Через местный форыч:</p>\n<pre><code class="language-cpp">for(const auto&#x26; p: list)\n{\n    cout &#x3C;&#x3C; p.name;\n}\n</code></pre>\n<h1>Управление ресурсами</h1>\n<p>В первую очередь нужно использовать RAII и контейнеры. Указатели стоит использовать лишь тогда, когда нужна именно семантика указателей, и даже в этом случае стоит по минимуму использовать <code>new</code> и <code>delete</code>, а вместо этого прибегать к смарт-указателям: <code>unique_ptr</code> и <code>shared_ptr</code>.</p>\n<h2>unique_ptr</h2>\n<pre><code class="language-cpp">1  void f(int i, int j)\n2  {\n3   X* p = new X;\n4   unique_ptr&#x3C;X> sp {new X};\n5\n6   if(i&#x3C;99) throw Z{};\n7   if(j&#x3C;77) return;\n8   p->do_something();\n9   sp->do_something();\n10  //...\n11  delete p;\n12 }\n</code></pre>\n<p>В строчках 6-9 может произойти преждевременный выход и тогда указатель <code>p</code> не освободится. А указатель <code>sp</code> гарантирует, что его объект будет уничтожен не зависимо от того, каким образом мы выйдем из функции.</p>\n<p>Кстати, если бы мы объявили <code>x</code> как <code>X x;</code>, то достигли бы того же эффекта.</p>\n<h2>shared_ptr</h2>\n<p>Семантика похожа на <code>unique_ptr</code>, только эти указатели копируются, а не перемещаются.</p>\n<pre><code class="language-cpp">void f(shared_ptr&#x3C;fstream>);\nvoid g(shared_ptr&#x3C;fstream>);\n\nvoid user(const string&#x26; name, ios_base::openmode mode)\n{\n    shared_ptr&#x3C;fstream> fp {new fstream(name, mode)};\n    if (!∗fp) throw No_file{}; // make sure the file was properly opened\n    f(fp);\n    g(fp);\n    // ...\n}\n</code></pre>\n<p>Файл, открытый конструктором <code>fp</code> будет уничтожен, когда последняя из использующих его функций (<code>user</code>, <code>f</code>, <code>g</code>) уничтожит свою копию указателя.</p>\n<p><code>shared_ptr</code> усложняет рассуждения о времени жизни объекта, поэтому его стоит использовать только когда точно нужно разделяемое владение.</p>\n<h1>Многопоточность</h1>\n<p>Чтобы создать новый поток, нужно создать объект <code>std::thread</code> и передать ему функтор в качестве аргумента:</p>\n<pre><code class="language-cpp">void f();\n\nstruct F {\n    void operator()();\n}\n\nvoid user()\n{\n    thread t1 {f};\n    thread t2 {F()};\n\n    t1.join();\n    t2.join();\n}\n</code></pre>\n<p>Если в функтор нужно передать аргументы, то они перечисляются как остальные аргументы <code>thread</code> после самого функтора:</p>\n<pre><code class="language-cpp">void f(int a);\n\nthread t1 {f, 42};\n</code></pre>\n<p>Если из потоков нужно что-то вернуть, то придется одним из аргументов передавать указатель на место, куда вернуть результат:</p>\n<pre><code class="language-cpp">void f(int a, int* res);\n\nint res;\nthread t1 {f, 52, &#x26;res};\n</code></pre>\n<h2>Синхронизация</h2>\n<h3>mutex</h3>\n<p>Вот так используем мютексы, чтобы синхронизировать доступ к разделяемой переменной из нескольких потоков:</p>\n<pre><code class="language-cpp">mutex m;\nint sh;\n\nvoid f() \n{\n    unique_lock&#x3C;mutex> lck{m};\n    sh+=7;\n}\n</code></pre>\n<p>Структура <code>unique_lock</code> поддерживает RAII, поэтому мютекс будет автоматически захвачен (<code>m.lock()</code>) в ее конструкторе и автоматически освобожден (<code>m.unlock()</code>) в ее деструкторе по выходу из функции <code>f</code>.</p>\n<h3>condition_variable</h3>\n<p>Механизм, позволяющий одному потоку ждать другого. В частности, позволяет потоку ждать выполнения некоего условия (собыия), которое выполняется как результат работы другого потока.</p>\n<p>Функция <code>wait(unique_lock&#x3C;mutex>&#x26; lock)</code> блокирует текущий поток, пока кто-то не вызовет <code>notify_one</code> или <code>notify_all</code>. Автоматически отпускает <code>lock</code>, а когда события дождались - захватывает его обратно. Иногда бывают ложные выстрелы события, поэтому лучше использовать в виде <code>while(mcond.wait(lck))</code>. Перед вызовом <code>wait</code> блокировка <code>lock</code> должна быть уже захвачена.</p>\n<p>В качестве второго параметра может принимать предикат, который будет автоматически проверяться для избежания ложных выстрелов события.</p>\n<p>На этом основании можно решить классичекую задачу producer-consumer:</p>\n<pre><code class="language-cpp">class Message { ...}\n\nqueue&#x3C;Message> mqueue;\ncondition_variable mcond;\nmutex mmutex;\n\nvoid consumer() \n{\n    while(true) {\n        unique_lock&#x3C;mutex> lck{mmutex};         // захватываем lck\n        while(mcond.wait(lck))  /*do nothing*/; // lck отпускается, а мы ждем события mcond\n                                                // когда дождались события mcond, lck автоматически захватывается вновь\n        auto m = mqueue.front();\n        mqueue.pop();\n        lck.unlock();                           // совсем отпускаем lck\n        // ... process m...\n    }\n}\n\nvoid producer() \n{\n    while(true) {\n        Message m;\n        unique_lock&#x3C;mutex> lck {mmutex};    // захватываем lck\n        mqueue.push(m);\n        mcond.notify_one();                 // "стреляем" событие, означающее, что в mqueue появились элементы\n                                            // lck автоматически отпускается\n    }\n}\n</code></pre>\n<h2>Таски</h2>\n<p>Есть несколько конструкций, более высокоуровневых, чем треды:</p>\n<ul>\n<li><code>future</code>, <code>promise</code></li>\n<li><code>packaged_task</code></li>\n<li><code>async()</code></li>\n</ul>\n<p>Все они находятся в заголовке <code>&#x3C;future></code>.</p>\n<h3><code>future</code> и <code>promise</code></h3>\n<p>Позволяют передавать значение между тасками без использования локов.</p>\n<p>Футуры получаются из промисов методом <code>get_future()</code>. В промис можно записать значение методом <code>set_value</code> или эксепшн методом <code>set_Exception</code>, а из футура потом его прочитать методом <code>get()</code>. При этом текущий поток будет заблокирован, пока результат не будет записан в соответствующий промис, а если в промис записан эксепшн, то <code>get()</code> выстрелит его же.</p>\n<pre><code class="language-cpp">void product(std::promise&#x3C;int>&#x26;&#x26; intPromise, int a, int b){\n  intPromise.set_value(a*b);\n}\n\nint main(){\n\n  int a= 20;\n  int b= 10;\n\n  std::promise&#x3C;int> prodPromise;\n\n  std::future&#x3C;int> prodResult= prodPromise.get_future();\n\n  std::thread prodThread(product,std::move(prodPromise),a,b);\n\n  std::cout &#x3C;&#x3C; "20*10= " &#x3C;&#x3C; prodResult.get() &#x3C;&#x3C; std::endl;\n\n  prodThread.join();\n}\n</code></pre>\n<h1>Утилиты</h1>\n<h2>Время</h2>\n<pre><code class="language-cpp">using namespace std::chrono;\n\nauto t0 = high_resolution_clock::now();\ndo_work();\nauto t1 = high_resolution_clock::now();\ncout &#x3C;&#x3C; duration_cast&#x3C;milliseconds>(t1-t0).count() &#x3C;&#x3C; "msec\\n";\n</code></pre>\n<h2>Type functions</h2>\n<p>Это такие функции, выполняемые во время компиляции и принимающие в качестве аргументов типы, или возвращающие типы.</p>\n<p>Примеры:</p>\n<pre><code class="language-cpp">constexpr float min = numeric_limits&#x3C;float>::min();\nconstexpr int szl = sizeof(int);\nvector&#x3C;decltype(*beg)> v;\n</code></pre>\n<p>Отдельно проходят предикаты над типами:</p>\n<pre><code class="language-cpp">bool b = is_arithmetic&#x3C;int>();\n</code></pre>\n<p>Еще есть <code>is_class</code>, <code>is_pod</code>, <code>is_literal_type</code>, <code>has_virtual_destructor</code>, и <code>is_base_of</code>.</p>\n<h3>iterator_traits</h3>\n<p>Это такая функция над типом, позволяющая получить некоторые свойства типа итератора.</p>\n<p>Пример:</p>\n<pre><code class="language-cpp">template&#x3C;class Iterator>\nvoid some_algorithm(Iterator start, Iterator end)\n{\n    typename std::iterator_traits&#x3C;Iterator>::value_type value = *start;  // так получили тип значения, используемого в итераторе Iterator\n    ...\n}\n</code></pre>\n<p>Структура <code>iterator_traits</code> содержит следующие поля:</p>\n<pre><code class="language-cpp">template &#x3C;class T>\nstruct iterator_traits {\n  typedef typename T::value_type value_type;\n  typedef typename T::difference_type difference_type;\n  typedef typename T::iterator_category iterator_category;\n  typedef typename T::pointer pointer;\n  typedef typename T::reference reference;\n}\n</code></pre>\n<h1>Макросы</h1>\n<p>Оператор <code>#</code> перед использованием аргумента обернет его в кавычки и будет использовать как строковый литерал.</p>\n<pre><code class="language-cpp">#define PRINT_TOKEN(token) printf(#token " is %s", token);\n\nchar* foo = "hello";\nPRINT_TOKEN(foo) // => printf("foo" " is %d" "hello")   => "foo is hello"\n</code></pre>\n<p>Оператор <code>##</code> берет два токена и объединяет их в один. Результат может быть названием переменной, класса, или любым другим идентификатором.</p>\n<pre><code class="language-cpp">#define FIT(function_index) fit##function_index(int X, int Y, int x, int y)\n\nint FIT(1)\n{\n    return (X / x) * (Y / y);\n}\n\nint FIT(2)\n{\n    return std::fmax(fit1(X, Y, x, y), fit1(X, Y, y, x));\n}\n</code></pre>',
frontmatter:{path:"/blog/cpp",title:"C++"}}},pathContext:{}}}});
//# sourceMappingURL=path---blog-cpp-954a5497543342229156.js.map